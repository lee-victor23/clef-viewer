Run `cargo build --release` from the project root and report the result.

On success, print the full path to the output binary: `target/release/clef-viewer.exe` and its file size.
On failure, print the compiler errors and suggest a fix if the cause is obvious.